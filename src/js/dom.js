export const selectDom = (name) => document.querySelector(name);
export const selectDomAll = (name) => document.querySelectorAll(name);
export const createDom = (name) => document.createElement(name);
